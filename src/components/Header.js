import React from "react";
import { Link } from "react-router-dom";
import Nav from "./Nav";

const Header = (props) => {
  return (
    <div>
      <Nav />
    </div>
  );
};

export default Header;
